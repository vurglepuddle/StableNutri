org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# The project is on E: and Flutter's plugin sources (pub cache) are on C:.
# Kotlin's incremental caches cannot relate paths across drives and flood the
# build with warnings, so incremental Kotlin compilation stays off.
kotlin.incremental=false
